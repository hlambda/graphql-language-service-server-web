/*
	This function sets or returns the persistant data,
	used to define default paths for .env and GraphQL config files.
*/

export const createPersistantStorageForExtension = (
  content: string,
  key: string
) => {
  return;
};

export default createPersistantStorageForExtension;
